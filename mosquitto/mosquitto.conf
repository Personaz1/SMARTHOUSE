listener 1883 0.0.0.0
protocol mqtt
allow_anonymous true

# persistence for MQTT broker state (retained messages, etc.)
persistence true
persistence_location /mosquitto/data/

# WebSocket listener for browser clients (UI). Use 9002 to avoid conflicts
listener 9002 0.0.0.0
protocol websockets

